[project]
name = "aioschwab"
version = "0.0.0"
description = "Asynchronous HTTP python framework for Schwab Individual Trader API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "ipykernel>=7.0.1",
    "jupyter>=1.1.1",
]
